The process_sequencing.py script takes the AB1 files from the raw_sequences folder and extracts forward and reverse sequences into fasta files within the clustering folder.
In the clustering folder, the script cluster_PvdD.bat performs clustering.
"iNEXT and Venn diagrams.R" is used to estimate OTU diversity and draw venn diagrams.